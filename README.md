# Calculator

Simple CLI calculator program built in Rust. Uses a custom parser and interpreter to parse calculation inputs and convert to a calculation AST, which can then be computed to get the result of the calculation if possible.
